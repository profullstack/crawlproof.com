node_modules
.next
# Ignore every env file, then allow the checked-in template back. Deliberately
# broad: the old list named each variant explicitly, so `.env.bak-2026-07-28`
# matched none of them and a full production environment landed in git.
.env*
!.env.example
*.log
*.mmdb
data/GeoLite2-*
.DS_Store
dist
worker/dist
playwright-cache
tsconfig.tsbuildinfo
*.tsbuildinfo
supabase/.temp/

# Claude Code local worktrees/state
.claude/
